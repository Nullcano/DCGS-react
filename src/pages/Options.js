import React from "react";
import Menu from "../components/Menu";

const Options = () => (
    <div className="Options">
        <Menu />
        <h1 class="fw1">Options</h1>
    </div>
)

export default Options;